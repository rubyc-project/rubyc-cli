# ---------- Pulled third-party sources (re-fetchable, never committed) ----------
/dotnet-runtime/

# ---------- Rust / Cargo ----------
/target/
**/*.rs.bk
**/*.rs.bk.*
Cargo.lock.bak
**/Cargo.lock.bak

# Generated artifacts from rubyc itself (compiled output during testing)
*.rbc
# ...but keep any intentionally tracked fixtures
!tests/fixtures/*.rbc

# ---------- Interop example build outputs (sources are tracked) ----------
examples/interop/**/*.so
examples/interop/chost
examples/interop/rhost
examples/interop/native_add/libnative_add.so

# ---------- Compiled C/C++ from examples & benchmarks (sources tracked) ----------
*.o
*.obj
*.dylib
*.dll
*.a
a.out
*.out
!tests/fixtures/*.out
benches/bin/
perf-out/

# Plugin build outputs (the .so the build auto-installs); keep the C fixture.
*.so
!examples/interop/native_add/libnative_add.so

# ---------- Build / tooling caches ----------
**/target/
dist/
out/
**/.cargo/
**/.rustup/

# ---------- Coverage / profiling ----------
*.profraw
*.profdata
*.d
coverage/
tarpaulin-report.html
lcov.info

# ---------- Local scratch / test outputs ----------
/tmp/
*.log
*.tmp
core
core.*

# ---------- IDEs / editors ----------
.idea
.idea/
.vscode
.vscode/
.devcontainer
.devcontainer/
*.swp
*.swo
*~
.project
.settings/
*.iml
*.code-workspace
*.sublime-workspace
*.sublime-project

# ---------- OS junk ----------
.DS_Store
Thumbs.db
desktop.ini
*.pyc
__pycache__/

# ---------- Agent / AI tooling state ----------
TODO.md
AGENTS.md
.kilo
.opencode
.sisyphus
.cursor
.cursor/
.claude
.claude/
.aider*
.rules
.rules/
CODEX.md
CLAUDE.md

# ---------- Secrets / env ----------
.env
.env.*
*.pem
*.key
id_rsa
credentials.json

# ---------- Misc local state ----------
*.pid
*.sock
nohup.out
scratch/
